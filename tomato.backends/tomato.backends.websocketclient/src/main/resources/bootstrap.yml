spring:
  application:
    name: tomato.backends.websocketclient
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    open-in-view: false
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
  liquibase:
    enabled: false
  main:
    web-application-type: none
server:
  port: 8079